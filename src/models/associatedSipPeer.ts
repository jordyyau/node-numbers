/**
 * bandwidthLib
 *
 * This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
 */

import { number, object, optional, Schema, string } from '../schema';

export interface AssociatedSipPeer {
  siteId?: number;
  siteName?: string;
  peerId?: number;
  peerName?: string;
}

export const associatedSipPeerSchema: Schema<AssociatedSipPeer> = object({
  siteId: ['siteId', optional(number()), { xmlName: 'SiteId' }],
  siteName: ['siteName', optional(string()), { xmlName: 'SiteName' }],
  peerId: ['peerId', optional(number()), { xmlName: 'PeerId' }],
  peerName: ['peerName', optional(string()), { xmlName: 'PeerName' }],
});
