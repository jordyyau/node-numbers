/**
 * bandwidthLib
 *
 * This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema';
import {
  SipPeerHttpSettings,
  sipPeerHttpSettingsSchema,
} from './sipPeerHttpSettings';

export interface SipPeerOriginationSettings {
  httpSettings?: SipPeerHttpSettings;
  voiceProtocol?: string;
}

export const sipPeerOriginationSettingsSchema: Schema<SipPeerOriginationSettings> = object(
  {
    httpSettings: [
      'httpSettings',
      optional(lazy(() => sipPeerHttpSettingsSchema)),
      { xmlName: 'HttpSettings' },
    ],
    voiceProtocol: [
      'voiceProtocol',
      optional(string()),
      { xmlName: 'VoiceProtocol' },
    ],
  }
);
