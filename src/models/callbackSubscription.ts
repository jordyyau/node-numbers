/**
 * bandwidthLib
 *
 * This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
 */

import { lazy, number, object, optional, Schema, string } from '../schema';
import {
  CallbackCredentials,
  callbackCredentialsSchema,
} from './callbackCredentials';

export interface CallbackSubscription {
  url?: string;
  expiry?: number;
  status?: string;
  callbackCredentials?: CallbackCredentials;
}

export const callbackSubscriptionSchema: Schema<CallbackSubscription> = object({
  url: ['url', optional(string()), { xmlName: 'URL' }],
  expiry: ['expiry', optional(number()), { xmlName: 'Expiry' }],
  status: ['status', optional(string()), { xmlName: 'Status' }],
  callbackCredentials: [
    'callbackCredentials',
    optional(lazy(() => callbackCredentialsSchema)),
    { xmlName: 'CallbackCredentials' },
  ],
});
