/**
 * bandwidthLib
 *
 * This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
 */

import { array, number, object, optional, Schema, string } from '../schema';

export interface NnRoute {
  nnId?: number;
  espid?: string;
  deliveryNetwork?: string;
  name?: string;
  trafficType?: string;
  provisionerIds?: string[];
}

export const nnRouteSchema: Schema<NnRoute> = object({
  nnId: ['nnId', optional(number()), { xmlName: 'Nnid' }],
  espid: ['espid', optional(string()), { xmlName: 'Espid' }],
  deliveryNetwork: [
    'deliveryNetwork',
    optional(string()),
    { xmlName: 'DeliveryNetwork' },
  ],
  name: ['name', optional(string()), { xmlName: 'Name' }],
  trafficType: ['trafficType', optional(string()), { xmlName: 'TrafficType' }],
  provisionerIds: [
    'provisionerIds',
    optional(array(string(), { xmlItemName: 'ProvisionerId' })),
    { xmlName: 'ProvisionerIds' },
  ],
});
