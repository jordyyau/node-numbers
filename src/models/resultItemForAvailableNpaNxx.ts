/**
 * bandwidthLib
 *
 * This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
 */

import { number, object, optional, Schema, string } from '../schema';

export interface ResultItemForAvailableNpaNxx {
  npa?: string;
  nxx?: string;
  quantity?: number;
  city?: string;
  state?: string;
}

export const resultItemForAvailableNpaNxxSchema: Schema<ResultItemForAvailableNpaNxx> = object(
  {
    npa: ['npa', optional(string()), { xmlName: 'Npa' }],
    nxx: ['nxx', optional(string()), { xmlName: 'Nxx' }],
    quantity: ['quantity', optional(number()), { xmlName: 'Quantity' }],
    city: ['city', optional(string()), { xmlName: 'City' }],
    state: ['state', optional(string()), { xmlName: 'State' }],
  }
);
