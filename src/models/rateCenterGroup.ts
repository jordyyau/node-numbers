/**
 * bandwidthLib
 *
 * This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
 */

import {
  array,
  boolean,
  number,
  object,
  optional,
  Schema,
  string,
} from '../schema';

export interface RateCenterGroup {
  vendor?: string;
  automatedPort?: boolean;
  rateCenter?: string;
  city?: string;
  state?: string;
  lata?: string;
  tiers?: number[];
  telephoneNumbers?: string[];
}

export const rateCenterGroupSchema: Schema<RateCenterGroup> = object({
  vendor: ['vendor', optional(string()), { xmlName: 'Vendor' }],
  automatedPort: [
    'automatedPort',
    optional(boolean()),
    { xmlName: 'AutomatedPort' },
  ],
  rateCenter: ['rateCenter', optional(string()), { xmlName: 'RateCenter' }],
  city: ['city', optional(string()), { xmlName: 'City' }],
  state: ['state', optional(string()), { xmlName: 'State' }],
  lata: ['lata', optional(string()), { xmlName: 'LATA' }],
  tiers: [
    'tiers',
    optional(array(number(), { xmlItemName: 'Tier' })),
    { xmlName: 'Tiers' },
  ],
  telephoneNumbers: [
    'telephoneNumbers',
    optional(array(string(), { xmlItemName: 'Tn' })),
    { xmlName: 'TnList' },
  ],
});
