/**
 * bandwidthLib
 *
 * This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema';
import { TokenScope, tokenScopeSchema } from './tokenScope';

export interface ApiToken {
  tokenId?: string;
  secret?: string;
  tokenScope?: TokenScope;
}

export const apiTokenSchema: Schema<ApiToken> = object({
  tokenId: ['tokenId', optional(string()), { xmlName: 'TokenId' }],
  secret: ['secret', optional(string()), { xmlName: 'Secret' }],
  tokenScope: [
    'tokenScope',
    optional(lazy(() => tokenScopeSchema)),
    { xmlName: 'TokenScope' },
  ],
});
