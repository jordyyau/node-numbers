/**
 * bandwidthLib
 *
 * This file was automatically generated by APIMATIC v2.0 ( https://apimatic.io ).
 */

import { lazy, number, object, optional, Schema, string } from '../schema';
import { TelephoneNumber, telephoneNumberSchema } from './telephoneNumber';

export interface Reservation {
  reservationId?: string;
  accountId?: number;
  reservationExpires?: number;
  reservedTn?: TelephoneNumber;
}

export const reservationSchema: Schema<Reservation> = object({
  reservationId: [
    'reservationId',
    optional(string()),
    { xmlName: 'ReservationId' },
  ],
  accountId: ['accountId', optional(number()), { xmlName: 'AccountId' }],
  reservationExpires: [
    'reservationExpires',
    optional(number()),
    { xmlName: 'ReservationExpires' },
  ],
  reservedTn: [
    'reservedTn',
    optional(lazy(() => telephoneNumberSchema)),
    { xmlName: 'ReservedTn' },
  ],
});
